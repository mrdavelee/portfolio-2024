# To better understand this file read this:
# https://docs.ionos.space/docs/github-actions-customization/

name: "Deploy Now: Execute Build"

on:
  workflow_call:
    inputs:
      site-url:
        required: true
        type: string
      branch-id:
        required: true
        type: string

env:
  DEPLOYMENT_FOLDER: ./

jobs:
# Please do not rename this job
  build-project:
    name: build portfolio-2024
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: vnode

      - name: Build Node assets
        env:
          CI: true
          NEXT_PUBLIC_EMAILJS_APP_PUBLIC_KEY: C0HynhZAiGl46CGDf
          NEXT_PUBLIC_EMAILJS_APP_SERVICE_ID: service_m99o0yo = 'service_m99o0yo'
          NEXT_PUBLIC_EMAILJS_APP_TEMPLATE_ID: template_n6jav7b
          NEXT_PUBLIC_RECAPTCHA_SITE_KEY: 6Lc4cY8qAAAAABcx-vkTVI15DxuCnepQTeFadbEb
          RECAPTURE_SECRET_KEY: ${{ secrets.RECAPTURE_SECRET_KEY }}
          SITE_URL: ${{ inputs.site-url }}
        run: npm run build

# Please do not touch the following action
      - name: Store deployment content
        uses: ionos-deploy-now/artifact-action@v1
        with:
          api-key: ${{ secrets.IONOS_API_KEY }}
          service-host: api-eu.ionos.space
          project-id: d0803668-385e-49af-9f4d-27de58ca9ebb
          branch-id: ${{ inputs.branch-id }}
          version: ${{ github.sha }}
          folder: ${{ env.DEPLOYMENT_FOLDER }}
          config-file: .deploy-now/portfolio-2024/config.yaml
          action: upload
